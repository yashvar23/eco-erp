{
  "name": "eco_app",
  "version": "1.0.0",
  "private": true,
  "description": "Frontend asset bundle metadata for ECO App",
  "scripts": {
    "build": "echo \"Assets are built via bench build --app eco_app\"",
    "watch": "echo \"Use bench watch for development asset rebuilds\""
  }
}
